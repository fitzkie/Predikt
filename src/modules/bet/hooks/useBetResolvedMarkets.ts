'use client'

import { useResolvedMarkets, type UseResolvedMarketsProps } from '@azuro-org/sdk'


const useBetResolvedMarkets = (props: UseResolvedMarketsProps) => {
  return useResolvedMarkets(props)
}

export default useBetResolvedMarkets
